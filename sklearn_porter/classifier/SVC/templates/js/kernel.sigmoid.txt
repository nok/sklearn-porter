
// tanh(y<x,x'>+r)
var kernels = Array.apply(null, Array({0})).map(Number.prototype.valueOf,0),
    kernel;
for (var i=0; i<{0}; i++) {{
    kernel = 0.;
    for (var j=0; j<{1}; j++) {{
        kernel += svs[i][j] * atts[j];
    }}
    kernels[i] = Math.tanh(({2} * kernel) + {3});
}}
