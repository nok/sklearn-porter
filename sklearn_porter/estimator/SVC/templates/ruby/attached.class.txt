class {class_name}

	def initialize (nClasses, vectors, coeffs, inters, weights, kernel, gamma, coef0, degree)
		@nClasses = nClasses
		@classes = Array.new(nClasses)
		for i in 0 ... nClasses
			@classes[i] = i
		end

		@vectors = vectors
		@coeffs = coeffs
		@inters = inters
		@weights = weights

		@kernel = kernel.upcase
		@gamma = gamma
		@coef0 = coef0
		@degree = degree
	end

	{method}

end

if ARGV.length == {n_features}

	# Features:
	features = ARGV.collect {{ |i| i.to_f }}

	# Parameters:
	{vectors}
	{coeffs}
	{inters}
	{weights}

	# Prediction:
	clf = {class_name}.new {n_classes}, vectors, coeffs, inters, weights, "{kernel}", {gamma}, {coef0}, {degree}
	estimation = clf.{method_name} features
	puts estimation

end